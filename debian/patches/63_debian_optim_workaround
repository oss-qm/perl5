Work around toolchain breakage on ia64, arm and powerpc - #247176

diff -Naur --exclude=debian perl-5.8.6.orig/cflags.SH perl-5.8.6/cflags.SH
--- perl-5.8.6.orig/cflags.SH	2002-09-30 20:59:07.000000000 +1000
+++ perl-5.8.6/cflags.SH	2005-05-21 15:28:57.000000000 +1000
@@ -107,8 +107,11 @@
     perlmain) ;;
     perly) ;;
     pp) ;;
-    pp_ctl) ;;
-    pp_hot) ;;
+    pp_ctl|pp_hot|pp_sort)
+	case $archname in
+	    arm-*|ia64-*|powerpc-*)
+		optimize=-O1;;
+	esac;;
     pp_pack) ;;
     pp_sys) ;;
     regcomp) ;;
diff -Naur --exclude=debian perl-5.8.6.orig/hints/linux.sh perl-5.8.6/hints/linux.sh
--- perl-5.8.6.orig/hints/linux.sh	2004-04-17 02:09:50.000000000 +1000
+++ perl-5.8.6/hints/linux.sh	2005-05-21 15:26:30.000000000 +1000
@@ -88,13 +88,6 @@
 # use -O2 by default ; -O3 doesn't seem to bring significant benefits with gcc
 '')
     optimize='-O2'
-    case "`uname -m`" in
-        ppc*)
-            # on ppc, it seems that gcc (at least gcc 3.3.2) isn't happy
-	    # with -O2 ; so downgrade to -O1.
-            optimize='-O1'
-        ;;
-    esac
     ;;
 esac
 
